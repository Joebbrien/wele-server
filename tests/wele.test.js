const request = require("supertest");
const server = require("../userApp");
const chai = require("chai");


describe("USER INTEGRATED TESTS",(done)=>{
  it("/create should create a new user into the system", async ()=>{
    console.log("Testing runing")
  });
  done;
})
